pipeline {
    agent any
     
    
    stages{
        stage('build Analysis') {
           steps {
               sh 'cd webapp && npm install && npm run build'
              
             }
          }
    }
}
